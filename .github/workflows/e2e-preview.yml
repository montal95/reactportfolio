name: E2E on Netlify Preview

on:
  deployment_status:

jobs:
  e2e-preview:
    name: Playwright vs Netlify Preview
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Runs against the Netlify deploy preview URL — no local dev server started.
    # playwright.config.js skips webServer when PLAYWRIGHT_BASE_URL is set.
    if: >
      github.event.deployment_status.state == 'success' &&
      github.event.deployment_status.environment == 'Preview'
    env:
      NEXT_TELEMETRY_DISABLED: '1'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests against preview
        run: npx playwright test --project=chromium
        env:
          PLAYWRIGHT_BASE_URL: ${{ github.event.deployment_status.target_url }}

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-preview
          path: playwright-report/
          retention-days: 14
